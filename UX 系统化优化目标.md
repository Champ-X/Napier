# Napier 桌面端 UI/UX 系统化优化目标

## 1. 范围

将 Napier Web 从“能力与证据控制台”优化为“围绕任务完成组织的桌面 Agent 工作台”。

本阶段只覆盖 1280px、1440px、1920px 桌面视口，不考虑移动端、窄屏和触控适配。

保留现有 React/Preact、Vite、Ledger、Trajectory、审批、恢复、Sandbox、插件和证据能力；不重写运行时，不照搬 Codex 或 DeepSeek Harness 的品牌外观。

## 2. 北极星体验

用户进入一个工作区后，应当能够：

1. 快速找到或创建任务。

2. 直接描述目标并开始运行。

3. 始终看清 Agent 当前在做什么、是否需要自己处理。

4. 优先看到结果、文件改动、验证情况和下一步。

5. 只在主动查看时接触原始事件、哈希、回执、实验和运行时细节。

6. 在同一任务上下文内完成批准、纠偏、停止、恢复、查看变更和继续执行。

最终体验应当克制、可信、精确，像一个持续工作的桌面工具，而不是展示页或管理后台。

## 3. 信息分层

所有前端信息分成四级：

- L0 任务主线：用户消息、Agent 结果、当前动作、待处理决策、Composer。

- L1 工作证据：计划进度、工具摘要、改动文件、Artifact、引用、测试结果、浏览器与进程状态。

- L2 审计信息：完整工具参数、原始事件、模型调用、Token、成本、哈希、回执。

- L3 管理与开发能力：模型配置、插件、信任、实验、评测、自动化配置和运行时调试。

展示规则：

- L0 始终出现在默认对话面。

- L1 在相关时以内联摘要或右侧证据栏出现。

- L2 只进入“轨迹/证据”视图。

- L3 进入设置、自动化或开发者模式，不进入普通任务默认路径。

## 4. 桌面布局

### 4.1 全局 Shell

保留左侧工作区与任务树，但统一为：

- 展开宽度约 240–252px。

- 折叠宽度约 64–72px。

- 清晰区分工作区、任务、当前状态和全局入口。

- 新建任务保持为侧栏唯一高强调按钮。

- 设置、自动化和开发者功能不与任务列表争夺视觉权重。

顶部主导航收敛为：

- 对话

- 任务

- 轨迹

“工作台”不再作为容纳所有功能的杂项入口。

### 4.2 主内容轴

建立唯一的桌面内容轴：

- 对话正文、状态栏和 Composer 必须共享同一个水平容器和中心线。

- 主阅读列建议为 760–840px。

- 1280、1440、1920px 下均不得出现正文靠左、输入框居中或标题与内容脱轴。

- 1920px 不直接拉宽正文；额外空间用于可选证据侧栏或保持对称留白。

- 当证据侧栏打开时，主列仍保持稳定，不产生明显横向跳动。

## 5. 对话页

### 5.1 任务状态

将当前三列状态区压缩为一条 40–48px 的任务状态栏：

- 状态图标与状态名称。

- 当前动作或最终结果。

- 运行时间。

- 必要时出现 Stop、继续、批准等操作。

- Token、费用、运行 ID、下一步等进入可展开详情。

移除默认路径中的产品试用记录器、内部 Gate、Receipt 等开发信息。

完成任务时显示独立的结果摘要：

- 完成了什么。

- 修改了哪些文件。

- 验证是否通过。

- 生成了哪些 Artifact。

- 剩余风险或未完成项。

- 建议下一步。

### 5.2 消息与活动

- 用户消息使用轻量背景区分，Agent 正文继续采用开放式排版。

- 正文默认字号不小于 15px，中文行高约 1.65–1.75。

- 每个 Turn 的连续工具动作聚合成一条活动摘要。

- 当前运行中的动作可以展开；已完成动作默认折叠。

- 原始工具参数、完整日志、哈希和事件类型不直接占据对话流。

- 文件改动、引用、测试和 Artifact 使用统一附件卡片。

- Branch、复制、定位等消息操作通过统一操作菜单呈现，不只依赖隐藏的 Hover 状态。

### 5.3 Composer

- Composer 与消息列严格对齐。

- 输入区支持自动增高，并保持清晰的最大高度。

- 主操作始终只有发送或停止。

- 模式、工作区和运行选项降为次级工具条。

- 禁用时必须显示明确原因，而不仅是降低透明度。

- 保留 Cmd/Ctrl+Enter、Steering、Follow-up 和 Stop 能力。

- 运行中保持位置稳定，不因警告、审批或准备状态发生明显跳动。

## 6. “任务”视图

将当前八个 Workbench 标签重新组织为最多四个任务级区域：

- 概览：目标、计划、当前阶段和最终结果。

- 变更：修改文件、Diff、Artifact、引用与验证结果。

- 运行环境：仅在存在活动 Browser、Process 或 Sandbox 时显示。

- 验证：测试、回放和与当前任务直接相关的证据。

调整能力归属：

- 自动化是工作区级能力，移出单次会话 Workbench。

- 实验室、评测、Fixture、Receipt 和信任治理进入开发者模式。

- 当前“文件”页面如果主要展示恢复/回收内容，应重命名为“恢复”；

  真正的“变更”页面应优先展示本次任务修改的文件和 Artifact。

- Goal 与 Plan 合并为一条任务叙事，避免两个入口表达相近概念。

- Browser 和 Process 没有活动实例时只显示紧凑空态，不保留巨大空白容器。

## 7. 轨迹页

保留 Trajectory 作为 Napier 的核心差异化界面，并进行统一化：

- 页面名称统一为“运行轨迹”，避免中文导航与英文大标题割裂。

- 保留输入、模型、工具三类来源和时间轴。

- 保留搜索、过滤、运行分组和 Key/All 切换。

- 去除无语义的红色装饰线；红色只用于失败或危险。

- 统计区、过滤器、时间轴和事件列表使用统一 Card、Tabs、Badge 与字体体系。

- 默认展示高信号动作；完整审计事件通过明确开关查看。

- 图表注释可使用 10–11px，其余可见文字不得低于 12px。

- 长轨迹必须使用服务端投影、增量数据或虚拟列表，不能依赖每次完整重放。

## 8. 设置与高级能力

设置应成为真正的设置页面或符合规范的模态窗口：

- 使用 `role="dialog"aria-modal="true"`、标题关联和焦点约束。

- Escape 关闭并恢复触发点焦点。

- 表单内容最大宽度控制在约 720–840px，不能横向铺满整个窗口。

- 左侧设置导航稳定，右侧表单按 Section、Field Group、Help Text 组织。

- Agent、模型、记忆、扩展、工作区和语言使用同一表单体系。

- 修改配置时明确显示未保存、保存中、成功和失败状态。

- 插件能力使用概要列表加详情抽屉，不用大量长卡片一次性铺开。

- 中英文 UI 必须完整一致；模型名、路径、插件 ID 等技术标识可以保留英文。

## 9. 视觉系统

建立 [`DESIGN.md`](http://DESIGN.md) 与集中式语义 Token，作为唯一视觉来源。

### 9.1 色彩

- 保留当前蓝色作为品牌/主操作色。

- 绿色只表示成功。

- 黄色或琥珀色只表示等待、警告或运行中。

- 红色只表示失败、危险和破坏性操作。

- 紫色、橙色等只在轨迹数据分类中使用。

- 所有组件只能引用语义 Token，不直接书写颜色值。

- 清除全部未定义 CSS 变量。

- 除 Token 文件和特殊可视化外，不允许新增 Hex、RGB、HSL 字面量。

### 9.2 字体

使用对中文友好的系统字体栈，例如：

- UI/正文：系统 Sans、PingFang SC、Noto Sans CJK SC、Microsoft YaHei。

- 代码、路径、模型 ID、哈希：等宽字体。

建议字号层级：

- 页面标题：28–32px。

- 区域标题：20–24px。

- 卡片标题：15–17px。

- 正文：14–16px。

- 控件与辅助文字：12–13px。

- 图表极小注释：最低 10–11px。

普通可见文字不得低于 12px。等宽字体只用于真正的技术数据，不用于所有导航和说明文字。

### 9.3 间距、圆角与阴影

- 使用统一 4/8px 间距节奏。

- 圆角只保留 6、10、14、18 四档。

- 按钮、输入框、卡片、Popover 和 Modal 各使用固定档位。

- 阴影只保留无阴影、浮起层、模态层三个级别。

- Composer 不应拥有与其他界面完全不同的超强阴影。

- 卡片背景以白色和中性灰为主，避免每个高级模块自行发明蓝灰、红线或渐变主题。

### 9.4 动效

- 动效只使用 opacity 和 transform。

- 常规反馈控制在 120–220ms。

- 页面切换不使用大幅位移。

- 支持 `prefers-reduced-motion`。

- 运行状态可使用克制的脉冲或旋转，不使用持续抢注意力的动画。

## 10. 组件与交互规范

建立可复用基础组件：

- Button / IconButton

- Tabs / SegmentedControl

- Card / Section

- Badge / StatusIndicator

- Field / Select / Textarea

- EmptyState / ErrorState / LoadingState

- ActivitySummary

- ArtifactCard

- Drawer / Dialog / Popover

- PageHeader / TaskStatusBar

所有交互组件必须覆盖：

- default

- hover

- focus-visible

- active

- disabled

- loading

- error（适用时）

- readonly（适用时）

桌面点击目标建议不小于 32px；关键主操作不小于 40px。焦点环必须清晰，不能只依赖颜色变化。

空态、错误态和加载态必须互斥。不得同时显示“加载失败”和“没有内容”。

## 11. 工程收口

- 将 `styles.css` 拆分为 Token、Global、Primitive、Layout 和 Feature 样式。

- 单个普通组件文件控制在 300 行以内。

- 页面容器只负责组合；数据获取、投影和交互逻辑进入 Hook/View Model。

- 拆分 ContextPanel、PlanPanel、AutomationPanel、ReceiptTrustPanel 等超大组件。

- 页面可见文案统一进入 i18n Copy 层，禁止组件继续硬编码用户可见英文。

- 为设计系统增加独立展示页，集中展示颜色、字体、按钮、输入、卡片、状态与空态。

- 为核心页面建立 1280、1440、1920px 视觉回归基线。

## 12. 性能体验

- Conversation 不得在每次渲染时从完整事件数组重算全部投影。

- 优先消费服务端完成投影；短期至少使用稳定 Memo、增量 Selector 与虚拟列表。

- 长对话、长轨迹和大量工具事件仍应保持平滑滚动。

- 切换任务、顶层视图和设置 Section 时不得出现明显主线程停顿。

- 实时流更新应批处理，避免每个 Delta 触发整棵对话树更新。

- 自动滚动只在用户位于底部附近时生效；用户回看历史时不得抢夺位置。

## 13. 验收场景

至少覆盖以下桌面场景：

1. 无任务的初始工作区。

2. 普通短任务。

3. 工具密集的长任务。

4. 正在运行且可 Steering。

5. 等待审批或浏览器接管。

6. 失败与可恢复状态。

7. 完成并产生文件、Diff、Artifact 和验证结果。

8. 多 Run 的完整 Trajectory。

9. Agent/模型设置。

10. 插件与高级能力页面。

验收标准：

- 1280、1440、1920px 下无意外裁切、重叠和脱轴。

- 消息列、状态区和 Composer 使用同一内容轴。

- 默认任务页面不展示原始事件、哈希、Receipt 或实验表单。

- 可见普通文字不小于 12px。

- 所有普通文字和控件满足 WCAG 2.2 AA 对比度。

- 不存在未定义 CSS 自定义变量。

- Token 文件外不存在新增颜色字面量。

- 中文模式下不出现未翻译的用户界面文案。

- 空、加载、错误、运行、等待、失败、恢复、完成状态均有唯一且明确的呈现。

- 键盘能够完成顶层导航、设置导航、Composer 操作、关闭 Dialog 和关键审批。

- 前端 TypeScript、单元测试、交互测试和视觉回归全部通过。

## 14. 实施优先级

### P0：统一默认任务体验

- 建立 Token、字体和基础组件。

- 修复 1920px 内容轴与 Composer 对齐。

- 收敛任务状态栏。

- 移除默认路径中的内部试用与治理信息。

- 聚合工具活动。

- 修复字号、对比度、未定义变量和中英文混排。

- 重构 Workbench 信息架构。

### P1：统一二级界面

- 完成任务详情、变更、运行环境和验证页面。

- 重做 Settings。

- 统一 Trajectory 与主产品视觉语言。

- 完善空态、错误态、审批、失败和恢复体验。

### P2：工程与体验稳定性

- 拆分超大组件和单体 CSS。

- 引入投影缓存、虚拟列表和批量流更新。

- 建立设计系统展示页和桌面视觉回归。

- 完成 WCAG、键盘与长任务性能验证。

## 15. 非目标

- 不考虑移动端。

- 不复制 Codex 或 DeepSeek Harness 的配色和组件外形。

- 不因 UI 优化重写 Ledger、Runtime 或安全模型。

- 不删除 Napier 的审计、回放、恢复、实验和治理能力。

- 不为了“更简洁”隐藏正在等待用户处理的审批、失败或风险。

- 不继续增加新的管理页面，除非现有信息架构无法承载且有真实任务需求。